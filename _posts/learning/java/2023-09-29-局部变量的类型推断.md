---
layout: post
title: 局部变量的类型推断（var）
author: deathwhispers
date: 2023-09-29
slug: local-variable-type-inference
categories:
- Java
tags:
- Java
type: note
status: draft
created: 2023-09-29 16:38
updated: 2026-04-20 18:30
week: 2025-W48
---

`var` 在 Java 10 引入，是"局部变量类型推断"，不是动态类型。

## 1. 基本用法

```java
var name = "java";      // 推断为 String
var list = new ArrayList<String>();
for (var item : list) {
    System.out.println(item);
}
```

## 2. 关键限制

- 只能用于局部变量。
- 必须在声明时初始化（`var a;` 不合法）。
- 不能用于类成员变量。
- 不能直接用 `null` 初始化。

## 3. 与 Lambda 的关系

Java 11 允许在 Lambda 参数中使用 `var`，常用于配合注解：

```java
Consumer<String> c = (@Nonnull var s) -> System.out.println(s.toUpperCase());
```

## 4. 使用建议

- 右值类型非常明显时使用 `var`。
- 业务语义不清晰时保留显式类型。
- 公共 API、核心模型代码中谨慎使用，优先可读性。

## 相关阅读

- [Java10 新特性概览](/posts/java10-new-features-overview/)
- [Java11 新特性概览](/posts/java11-new-features-overview/)
- [Java 版本新特性学习索引（9~16）](/posts/java-version-features-study-index/)
